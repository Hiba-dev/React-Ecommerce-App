import React, { PureComponent } from 'react'

function Title({title}){
   
        return (
            <h1 className="title text-center m-5">{title}</h1>
        )
    }

export default Title